1 - O método "SyncService.performBootSync" também é um dos que chama o "MobileObjectDatabase.getInstance().bootup(deviceService)",
	que pode acabar excluindo dados não sincronizados. Não foi aplicada a mesma correção feita no "SyncEngine.startBootSync"
	por que acredita-se que este método seja chamado apenas na inicialização. 
	Monitorar o seu comportamento e aplicar uma outra correção caso seja necessário, pois se houve um erro no bootup inicial,
	ele realmente deve apagar os dados para depois poder fazer o bootup novamente.
	
2 - Verificar se a correção do "SyncEngine.startBootSync" funciona também para registros que apenas alterados (não novos),
	porque se o registro que vier do bootup sobrescrever o registro alterado, as alterações serão perdidas.
	
3 - Verificar se a correção do "SyncAdapter.resetSyncState" não interfere no contorno do próprio OpenMobster para o problema
	de mapa de registros não salvos, pois a correção faz com que o channel continue bootado. Se o contorno do OpenMobster
	depender do channel não estar bootado para aí então executar o bootup, haverá problemas.